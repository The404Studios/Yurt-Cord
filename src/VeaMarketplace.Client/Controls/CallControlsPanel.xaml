<UserControl x:Class="VeaMarketplace.Client.Controls.CallControlsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Call Button Base Style with Animations -->
        <Style x:Key="CallControlButton" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Background" Value="#4F545C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="28"
                                RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1.1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1.1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ColorAnimation Storyboard.TargetName="border"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#5D6269" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1" Duration="0:0:0.15">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ColorAnimation Storyboard.TargetName="border"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#4F545C" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3C3F44"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active State Button -->
        <Style x:Key="CallControlButtonActive" TargetType="Button" BasedOn="{StaticResource CallControlButton}">
            <Setter Property="Background" Value="#5865F2"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4752C4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button -->
        <Style x:Key="CallControlButtonDanger" TargetType="Button" BasedOn="{StaticResource CallControlButton}">
            <Setter Property="Background" Value="#ED4245"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C73E3E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Small Control Button -->
        <Style x:Key="CallControlButtonSmall" TargetType="Button" BasedOn="{StaticResource CallControlButton}">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#1E1F22" CornerRadius="16" Padding="20,16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Call Info -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center"
                        Margin="0,0,0,16">
                <TextBlock x:Name="CallTypeIcon" Text="📞" FontSize="16"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="CallStatusText" Text="In Call" FontSize="14"
                           Foreground="#B5BAC1" VerticalAlignment="Center"/>
                <TextBlock Text=" • " FontSize="14" Foreground="#72767D"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="CallDurationText" Text="00:00" FontSize="14"
                           Foreground="White" FontWeight="Medium"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Main Controls -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Mute Button -->
                <Grid Margin="0,0,12,0">
                    <Button x:Name="MuteButton" Click="Mute_Click" ToolTip="Toggle Mute">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=MuteButton, Path=Tag}" Value="True">
                                        <Setter Property="Background" Value="#ED4245"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock x:Name="MuteIcon" Text="🎤" FontSize="22"/>
                    </Button>
                    <TextBlock Text="Mute" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>

                <!-- Deafen Button -->
                <Grid Margin="0,0,12,0">
                    <Button x:Name="DeafenButton" Click="Deafen_Click" ToolTip="Toggle Deafen">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=DeafenButton, Path=Tag}" Value="True">
                                        <Setter Property="Background" Value="#ED4245"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock x:Name="DeafenIcon" Text="🔊" FontSize="22"/>
                    </Button>
                    <TextBlock Text="Deafen" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>

                <!-- Video Button -->
                <Grid Margin="0,0,12,0">
                    <Button x:Name="VideoButton" Click="Video_Click" ToolTip="Toggle Video">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=VideoButton, Path=Tag}" Value="True">
                                        <Setter Property="Background" Value="#5865F2"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock x:Name="VideoIcon" Text="📷" FontSize="22"/>
                    </Button>
                    <TextBlock Text="Video" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>

                <!-- Screen Share Button -->
                <Grid Margin="0,0,12,0">
                    <Button x:Name="ScreenShareButton" Click="ScreenShare_Click" ToolTip="Share Screen">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=ScreenShareButton, Path=Tag}" Value="True">
                                        <Setter Property="Background" Value="#43B581"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="🖥" FontSize="22"/>
                    </Button>
                    <TextBlock Text="Share" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>

                <!-- More Options Button -->
                <Grid Margin="0,0,20,0">
                    <Button x:Name="MoreButton" Style="{StaticResource CallControlButton}"
                            Click="More_Click" ToolTip="More Options">
                        <TextBlock Text="⚙" FontSize="22"/>
                    </Button>
                    <TextBlock Text="More" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>

                <!-- End Call Button -->
                <Grid>
                    <Button Style="{StaticResource CallControlButtonDanger}"
                            Width="80" Click="EndCall_Click" ToolTip="Leave Call">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📞" FontSize="20" RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="135"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                    <TextBlock Text="Leave" FontSize="10" Foreground="#72767D"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom"
                               Margin="0,0,0,-18"/>
                </Grid>
            </StackPanel>

            <!-- Secondary Controls -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                        Margin="0,28,0,0">
                <!-- Noise Suppression -->
                <Button x:Name="NoiseSuppressionButton" Style="{StaticResource CallControlButtonSmall}"
                        Click="NoiseSuppression_Click" ToolTip="Noise Suppression"
                        Margin="0,0,8,0">
                    <TextBlock Text="🔇" FontSize="16"/>
                </Button>

                <!-- Push to Talk -->
                <Button x:Name="PushToTalkButton" Style="{StaticResource CallControlButtonSmall}"
                        Click="PushToTalk_Click" ToolTip="Push to Talk"
                        Margin="0,0,8,0">
                    <TextBlock Text="🎙" FontSize="16"/>
                </Button>

                <!-- Picture in Picture -->
                <Button x:Name="PipButton" Style="{StaticResource CallControlButtonSmall}"
                        Click="PictureInPicture_Click" ToolTip="Picture in Picture"
                        Margin="0,0,8,0">
                    <TextBlock Text="📺" FontSize="16"/>
                </Button>

                <!-- Fullscreen -->
                <Button x:Name="FullscreenButton" Style="{StaticResource CallControlButtonSmall}"
                        Click="Fullscreen_Click" ToolTip="Fullscreen"
                        Margin="0,0,8,0">
                    <TextBlock Text="⛶" FontSize="16"/>
                </Button>

                <!-- Add Participants -->
                <Button x:Name="AddParticipantsButton" Style="{StaticResource CallControlButtonSmall}"
                        Click="AddParticipants_Click" ToolTip="Add Participants">
                    <TextBlock Text="➕" FontSize="16"/>
                </Button>
            </StackPanel>

            <!-- More Options Popup -->
            <Border x:Name="MoreOptionsPopup" Grid.Row="1" Visibility="Collapsed"
                    Background="#2B2D31" CornerRadius="8" Padding="8"
                    VerticalAlignment="Top" HorizontalAlignment="Center"
                    Margin="0,-180,0,0">
                <StackPanel Width="200">
                    <Button Click="SoundSettings_Click" Margin="0,0,0,4">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Padding" Value="12,0"/>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔊" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Sound Settings" FontSize="13"/>
                        </StackPanel>
                    </Button>

                    <Button Click="VideoSettings_Click" Margin="0,0,0,4">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Padding" Value="12,0"/>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📷" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Video Settings" FontSize="13"/>
                        </StackPanel>
                    </Button>

                    <Button Click="CallQuality_Click">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource CallControlButton}">
                                <Setter Property="Width" Value="Auto"/>
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="Padding" Value="12,0"/>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Call Quality" FontSize="13"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
