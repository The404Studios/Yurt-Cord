<UserControl x:Class="VeaMarketplace.Client.Views.NotificationCenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Colors.xaml"/>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryDark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryDark}" Padding="16" BorderBrush="{StaticResource TertiaryDark}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="🔔 Notifications" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>

                <Button Grid.Column="1" Content="Mark All Read" Style="{StaticResource SecondaryButton}"
                        Command="{Binding MarkAllReadCommand}" Padding="12,6" FontSize="12" Margin="0,0,8,0"/>
                <Button Grid.Column="2" Content="Settings" Style="{StaticResource IconButton}"
                        Command="{Binding OpenSettingsCommand}" Width="32" Height="32" FontSize="16"/>
            </Grid>
        </Border>

        <!-- Filter Tabs -->
        <Border Grid.Row="1" Background="{StaticResource SecondaryDark}" Padding="12,8" BorderBrush="{StaticResource TertiaryDark}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="All" Style="{StaticResource TabRadioButton}" IsChecked="True"
                             Command="{Binding FilterCommand}" CommandParameter="All" Margin="0,0,8,0"/>
                <RadioButton Content="Unread" Style="{StaticResource TabRadioButton}"
                             Command="{Binding FilterCommand}" CommandParameter="Unread" Margin="0,0,8,0"/>
                <RadioButton Content="Friends" Style="{StaticResource TabRadioButton}"
                             Command="{Binding FilterCommand}" CommandParameter="Friends" Margin="0,0,8,0"/>
                <RadioButton Content="Messages" Style="{StaticResource TabRadioButton}"
                             Command="{Binding FilterCommand}" CommandParameter="Messages" Margin="0,0,8,0"/>
                <RadioButton Content="Mentions" Style="{StaticResource TabRadioButton}"
                             Command="{Binding FilterCommand}" CommandParameter="Mentions"/>
            </StackPanel>
        </Border>

        <!-- Notifications List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Notifications}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SecondaryDark}"
                                Padding="16,12" BorderBrush="{StaticResource TertiaryDark}" BorderThickness="0,0,0,1"
                                MouseEnter="NotificationItem_MouseEnter" MouseLeave="NotificationItem_MouseLeave"
                                Cursor="Hand"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                    <TranslateTransform X="0" Y="0"/>
                                </TransformGroup>
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1" Duration="0:0:0.25"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.X)"
                                                             From="30" To="0" Duration="0:0:0.3">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.NotificationClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRead}" Value="False">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="#2F3136" Opacity="1"/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource TertiaryDark}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon -->
                                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20"
                                        VerticalAlignment="Top" Margin="0,0,12,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="FriendRequest">
                                                    <Setter Property="Background" Value="{StaticResource Blurple}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Message">
                                                    <Setter Property="Background" Value="{StaticResource Green}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Mention">
                                                    <Setter Property="Background" Value="{StaticResource Orange}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="ProductSold">
                                                    <Setter Property="Background" Value="{StaticResource Cyan}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="OrderUpdate">
                                                    <Setter Property="Background" Value="{StaticResource Pink}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="System">
                                                    <Setter Property="Background" Value="{StaticResource Red}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <TextBlock Text="{Binding Icon}" FontSize="20" Foreground="White"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Content -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                                               Foreground="White" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Message}" FontSize="12"
                                               Foreground="{StaticResource TextSecondary}"
                                               TextWrapping="Wrap" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding CreatedAt, Converter={StaticResource TimeAgoConverter}}"
                                               FontSize="11" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Unread Indicator -->
                                <Ellipse Grid.Column="2" Width="8" Height="8" Fill="{StaticResource Blurple}"
                                         VerticalAlignment="Center" Margin="8,0,0,0"
                                         Visibility="{Binding IsRead, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding HasNotifications, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <TextBlock Text="🔔" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
            <TextBlock Text="No notifications" FontSize="16" FontWeight="SemiBold"
                       Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,12,0,4"/>
            <TextBlock Text="You're all caught up!" FontSize="13"
                       Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
