<Window x:Class="VeaMarketplace.Client.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="150" Width="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Background="#2F3136"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0"
                   Foreground="White"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1"
                 Background="#40444B"
                 Foreground="White"
                 BorderBrush="#5865F2"
                 Padding="8,5"
                 FontSize="14"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="75" Height="30"
                    Margin="0,0,10,0" Click="Cancel_Click"
                    Background="#4F545C" Foreground="White"
                    BorderThickness="0"/>
            <Button Content="OK" Width="75" Height="30"
                    Click="OK_Click" IsDefault="True"
                    Background="#5865F2" Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
