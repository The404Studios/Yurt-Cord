<Window x:Class="VeaMarketplace.Client.Views.OverseerOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overseer Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Flying Message Animation -->
        <Storyboard x:Key="FlyAcrossScreen">
            <DoubleAnimation Storyboard.TargetProperty="(Canvas.Left)"
                             Duration="0:0:4">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseInOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.5"
                             BeginTime="0:0:3.5"/>
        </Storyboard>
    </Window.Resources>

    <!-- Main Canvas for flying elements -->
    <Canvas x:Name="OverlayCanvas" ClipToBounds="False">
        <!-- Flying messages and notifications will be added here dynamically -->
    </Canvas>
</Window>
